# Changelog — self-improve-minor-skill-creator

All notable minor edits to this skill. Newest first, one line per logical step.
Each line: bump level (PATCH | MINOR) + what changed + the friction it removes.
Dates are bashdates. Append-only — corrections are new entries. A MAJOR change is
out of scope here → use `skill-creator`.

## [1.1.0] — 2026-06-08
- MINOR: add the `## Pending signals` log convention so momentum (the ~3× rule)
  survives across sessions — audit flagged the rule had no tracking mechanism.
- PATCH: add a "Worked example — one full step" section — audit flagged the loop
  was prescriptive but had no concrete before/after diff.

## [1.0.0] — 2026-06-08
- Initial skill. Encodes the learning-rate discipline for minor skill edits, the
  minor/major gate, the edit loop, and the version + CHANGELOG protocol. Dogfoods
  its own versioning convention.

## Pending signals

<!-- One line per observed-but-not-yet-encoded friction: `date — "quote" — Nth`.
     Promote to a released entry above once a line reaches ~3 occurrences.
     Prune lines that never recur — that's the noise filter. -->
- (none)
