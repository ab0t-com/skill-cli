# Changelog — <skill-name>

All notable minor edits to this skill. Newest first, one line per logical step.
Each line: bump level (PATCH | MINOR) + what changed + the friction it removes.
Dates are bashdates ($(date +%Y-%m-%d)). Append-only — corrections are new entries.
A MAJOR change is out of scope here → use `skill-creator`.

## [1.0.0] — <YYYY-MM-DD>
- Adopt versioning. Baseline of the existing skill (no behavior change).

<!--
Entry shapes:

## [1.0.1] — <YYYY-MM-DD>
- PATCH: <what> — <friction it removes>.

## [1.1.0] — <YYYY-MM-DD>
- MINOR: <what was added> — <recurring gap it closes (note the ~2-3x recurrence)>.
-->

## Pending signals

<!-- One line per observed-but-not-yet-encoded friction: `date — "quote" — Nth`.
     Promote to a released entry above once a line reaches ~3 occurrences (momentum).
     Prune lines that never recur — that's the noise filter. -->
- (none)
